(require lispy.macros *)

(def fact [n :ret 1]
  (if (< n 2)
      (return ret)
      (return (fact (- n 1) (* ret n)))))

(def digit-sum [n]
  (= ret 0)
  (while n
    (+= ret (% n 10))
    (//= n 10))
  (return ret))

(print (digit-sum (fact 100)))