(require lispy.macros *)

(def sol [n]
  (= mem [1])
  (= rst 0)
  (for i (range n)
    (for j (range (- (len mem) 1) 0 -1)
      (+= (sub mem j) (sub mem (- j 1)))
      (if (> (sub mem j) 1000000)
          (+= rst 1)))
    (mem.append 1))
  (return rst))

(print (sol 100))
